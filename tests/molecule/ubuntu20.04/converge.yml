---
- name: Converge
  hosts: all
  become: true
  become_user: root
  vars_files:
    - ../../vars.yml
  tasks:
    - name: "Inlcude common"
      include_role:
        name: "common"
    - name: "Include db"
      include_role:
        name: "db"
